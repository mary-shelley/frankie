language: php
php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm

before_script:
  - composer selfupdate
  - composer install --prefer-source --no-interaction
script:
  - ./vendor/bin/phpunit
  - ./vendor/bin/phpspec run -fpretty
